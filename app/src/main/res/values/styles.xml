<!-- Crea un archivo llamado res/values/styles.xml -->
<resources>
    <style name="MainIconButton" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/textoPrincipal</item>
        <item name="android:textSize">16sp</item>
        <!-- Puedes agregar otros atributos de estilo según necesites -->
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:background">@drawable/btn_styles</item>
        <item name="android:padding">12dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>
    <style name="BotonCircular">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:background">@drawable/btn_styles</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:padding">12dp</item>
        <item name="android:elevation">8dp</item>
    </style>

</resources>
